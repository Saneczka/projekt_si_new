<nav class="navbar navbar-expand-lg navbar-light bg-light" style="margin-bottom: 1em;">
    <div class="container-fluid">
        <a class="nav-link" href="{{ url('album_index') }}">{{ 'menu_album'|trans }}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link{% if pathInfo == '/image/' %} active{% endif %}" aria-current="page" href="{{ url('image_index') }}">{{ 'menu_image'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link{% if pathInfo == '/comment/' %} active{% endif %}" href="{{ url('comment_index') }}">{{ 'menu_comments'|trans }}</a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item" style="float: right;">
                    <a class="nav-link" href="{{ url('app_logout') }}">{{ 'menu_logout'|trans }}</a>
                </li>
                {% else %}
                <li class="nav-item" style="float: right;">
                    <a class="nav-link" href="{{ url('app_login') }}">{{ 'menu_login'|trans }}</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>